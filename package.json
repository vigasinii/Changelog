{
  "name": "changelog-builder",
  "version": "1.0.0",
  "description": "Write and publish changelogs with shareable public URLs",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
